extends layout;
h1 Add Record
  form
    .form-group
      label(for="name") Name
      input.form-control#name(type="text")
    .form-group
      label(for="url") URL
      input.form-control#url(type="text")
    button(type="submit") Add