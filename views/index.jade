extends layout
block content
  .container
    h1 Records
    table.table.table-hover
      thead
        tr
          th Name
          th URL
          th Date Added
      tbody
        each record in records
          tr
            td= record.name
            td= record.url
            td= record.addedAt

    a.btn.btn-default(href="/new") Add

