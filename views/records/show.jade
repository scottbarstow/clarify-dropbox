extends ../layout
block content
  .container
    h2=record.name
    input#tags(type="text", data-role="tagsinput", placeholder='Tags', value=tags)
    input#record(type="hidden", value=record._id)
block scripts
  script(src='/js/tags.js')
